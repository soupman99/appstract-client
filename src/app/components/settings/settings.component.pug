.container
    form
        .form-group
            label(for='url') Url
            input#url.form-control(type='text', [(ngModel)]="url", name="url", placeholder="http://localhost:4300")
        button.btn.btn-success((click)="saveUrl(url)") Update
